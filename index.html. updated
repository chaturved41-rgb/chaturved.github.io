```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cheap & Best Saloon - Professional Barber Services</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Poppins', sans-serif;
            line-height: 1.6;
            color: #333;
            overflow-x: hidden;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        /* Header */
        header {
            background: linear-gradient(135deg, #2c1810 0%, #4a2c1a 100%);
            color: white;
            padding: 1rem 0;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            box-shadow: 0 2px 20px rgba(0,0,0,0.3);
            transition: all 0.3s ease;
        }
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .logo {
            font-size: 1.8rem;
            font-weight: 700;
            color: #ffd700;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        .nav-links {
            display: flex;
            list-style: none;
            gap: 2rem;
        }
        .nav-links a {
            color: white;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s;
            position: relative;
        }
        .nav-links a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -5px;
            left: 0;
            background: #ffd700;
            transition: width 0.3s;
        }
        .nav-links a:hover::after {
            width: 100%;
        }
        .nav-links a:hover {
            color: #ffd700;
        }
        .cta-btn {
            background: linear-gradient(45deg, #ffd700, #ffed4e);
            color: #2c1810;
            padding: 0.8rem 1.8rem;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
            box-shadow: 0 4px 15px rgba(255,215,0,0.4);
        }
        .cta-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(255,215,0,0.6);
        }
        .mobile-menu {
            display: none;
            flex-direction: column;
            gap: 1rem;
            cursor: pointer;
        }
        .mobile-menu span {
            width: 30px;
            height: 3px;
            background: #ffd700;
            transition: 0.3s;
        }
        /* Hero Section */
        .hero {
            background: linear-gradient(135deg, rgba(44,24,16,0.9), rgba(74,44,26,0.8)), 
                        url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1920 1080"><defs><radialGradient id="a" cx="50%" cy="50%" r="50%"><stop offset="0%" stop-color="%23ffd700"/><stop offset="100%" stop-color="%23ff8c00"/></radialGradient></defs><rect width="1920" height="1080" fill="%232c1810"/><circle fill="url(%23a)" cx="400" cy="300" r="200"/><circle fill="url(%23a)" cx="1520" cy="250" r="150"/><path fill="%234a2c1a" d="M0 600Q400 300 960 600T1920 600V1080H0z"/></svg>');
            background-size: cover;
            background-position: center;
            min-height: 100vh;
            display: flex;
            align-items: center;
            text-align: center;
            color: white;
            position: relative;
            margin-top: 80px;
        }
        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="%23ffd700" opacity="0.1"/><circle cx="75" cy="75" r="1.5" fill="%23ffd700" opacity="0.1"/><circle cx="50" cy="10" r="0.8" fill="%23ffed4e" opacity="0.2"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
            animation: sparkle 20s linear infinite;
        }
        @keyframes sparkle {
            0%, 100% { opacity: 0.3; }
            50% { opacity: 0.7; }
        }
        .hero-content {
            position: relative;
            z-index: 2;
            max-width: 800px;
        }
        .hero h1 {
            font-size: clamp(2.5rem, 5vw, 4rem);
            margin-bottom: 1.5rem;
            text-shadow: 3px 3px 10px rgba(0,0,0,0.7);
            animation: fadeInUp 1s ease;
        }
        .hero p {
            font-size: 1.4rem;
            margin-bottom: 2.5rem;
            animation: fadeInUp 1s ease 0.2s both;
        }
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        /* Services Section */
        .services {
            padding: 120px 0;
            background: linear-gradient(135deg, #f8f5f0 0%, #ede1d4 100%);
        }
        .section-title {
            text-align: center;
            font-size: clamp(2rem, 4vw, 3rem);
            margin-bottom: 4rem;
            color: #2c1810;
            position: relative;
        }
        .section-title::after {
            content: '';
            width: 100px;
            height: 4px;
            background: linear-gradient(45deg, #ffd700, #ffed4e);
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 2px;
        }
        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 2.5rem;
        }
        .service-card {
            background: white;
            padding: 2.5rem 2rem;
            border-radius: 20px;
            text-align: center;
            box-shadow: 0 15px 40px rgba(0,0,0,0.1);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            border: 1px solid rgba(255,215,0,0.1);
            position: relative;
            overflow: hidden;
        }
        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,215,0,0.1), transparent);
            transition: left 0.5s;
        }
        .service-card:hover::before {
            left: 100%;
        }
        .service-card:hover {
            transform: translateY(-15px) scale(1.02);
            box-shadow: 0 25px 60px rgba(0,0,0,0.2);
        }
        .service-icon {
            font-size: 4rem;
            color: #ffd700;
            margin-bottom: 1.5rem;
            display: block;
        }
        .service-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: #2c1810;
        }
        /* Partnership Section */
        .partnership {
            padding: 120px 0;
            background: linear-gradient(135deg, #ffd700 0%, #ffed4e 50%, #ffc107 100%);
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        .partnership::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.2) 0%, transparent 70%);
            animation: rotate 20s linear infinite;
        }
        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        .partnership-content {
            position: relative;
            z-index: 2;
        }
        /* Barbers & Products */
        .barbers, .products {
            padding: 120px 0;
        }
        .barbers {
            background: #f8f5f0;
        }
        .products {
            background: white;
        }
        .barbers-grid, .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2.5rem;
            margin-top: 4rem;
        }
        .barber-card, .product-card {
            text-align: center;
            padding: 2.5rem 1.5rem;
            background: white;
            border-radius: 20px;
            box-shadow: 0 15px 40px rgba(0,0,0,0.1);
            transition: all 0.4s ease;
        }
        .barber-card:hover, .product-card:hover {
            transform: translateY(-10px);
        }
        .barber-photo, .product-image {
            width: 160px;
            height: 160px;
            border-radius: 50%;
            margin: 0 auto 1.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 4rem;
            box-shadow: 0 15px 40px rgba(0,0,0,0.2);
        }
        .barber-photo {
            background: linear-gradient(135deg, #2c1810, #4a2c1a, #6b3f27);
            color: #ffd700;
            border: 4px solid #ffd700;
        }
        .product-image {
            background: linear-gradient(135deg, #ffd700, #ffed4e);
            color: #2c1810;
            font-size: 3rem;
        }
        /* Contact Section */
        .contact {
            padding: 120px 0;
            background: linear-gradient(135deg, #2c1810 0%, #4a2c1a 100%);
            color: white;
            text-align: center;
        }
        .contact form {
            max-width: 700px;
            margin: 3rem auto;
            display: grid;
            gap: 1.5rem;
        }
        .contact input, .contact textarea {
            padding: 1.2rem;
            border: 2px solid rgba(255,255,255,0.2);
            border-radius: 15px;
            font-family: inherit;
            background: rgba(255,255,255,0.1);
            color: white;
            font-size: 1rem;
            transition: all 0.3s;
        }
        .contact input::placeholder, .contact textarea::placeholder {
            color: rgba(255,255,255,0.7);
        }
        .contact input:focus, .contact textarea:focus {
            outline: none;
            border-color: #ffd700;
            background: rgba(255,255,255,0.2);
            transform: scale(1.02);
        }
        .contact button {
            background: linear-gradient(45deg, #ffd700, #ffed4e);
            color: #2c1810;
            padding: 1.2rem 3rem;
            border: none;
            border-radius: 50px;
            font-weight: 700;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.3s;
            box-shadow: 0 10px 30px rgba(255,215,0,0.4);
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .contact button:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(255,215,0,0.6);
        }
        .contact-info {
            margin-top: 4rem;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            font-size: 1.2rem;
        }
        .contact-info p {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 1rem;
            padding: 1rem;
            background: rgba(255,255,255,0.1);
            border-radius: 15px;
            transition: transform 0.3s;
        }
        .contact-info p:hover {
            transform: translateY(-5px);
        }
        /* Footer */
        footer {
            background: #1a0f0a;
            color: #ccc;
            text-align: center;
            padding: 3rem 0;
            border-top: 1px solid #333;
        }
        /* Mobile Menu */
        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }
            .mobile-menu {
                display: flex;
            }
            .hero h1 {
                font-size: 2.5rem;
            }
            .services-grid, .barbers-grid, .products-grid {
                grid-template-columns: 1fr;
            }
        }
        /* WhatsApp Button */
        .whatsapp-float {
            position: fixed;
            width: 60px;
            height: 60px;
            bottom: 40px;
            right: 40px;
            background: linear-gradient(45deg, #25d366, #128c7e);
            color: white;
            border-radius: 50px;
            text-align: center;
            font-size: 30px;
            box-shadow: 2px 2px 3px #999;
            z-index: 100;
            animation: bounce 2s infinite;
            display: flex;
            align-items: center;
            justify-content: center;
            text-decoration: none;
        }
        .whatsapp-float:hover {
            text-decoration: none;
            color: white;
            transform: scale(1.1);
        }
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
            40% { transform: translateY(-10px); }
            60% { transform: translateY(-5px); }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <nav class="container">
            <div class="logo">
                <i class="fas fa-cut"></i>
                Cheap & Best Saloon
            </div>
            <ul class="nav-links">
                <li><a href="#home">Home</a></li>
                <li><a href="#services">Services</a></li>
                <li><a href="#barbers">Barbers</a></li>
                <li><a href="#products">Products</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
            <a href="#contact" class="cta-btn">Book Now</a>
            <div class="mobile-menu">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </nav>
    </header>

    <!-- Hero Section -->
    <section id="home" class="hero">
        <div class="hero-content container">
            <h1>Cheap & Best Saloon</h1>
            <p>🏆 Professional barbers • Official Beardo Partner • Premium branded products • Expert hair checkups • Unmatched service quality</p>
            <a href="#services" class="cta-btn">Explore Services</a>
        </div>
    </section>

    <!-- Services Section -->
    <section id="services" class="services">
        <div class="container">
            <h2 class="section-title">Premium Grooming Services</h2>
            <div class="services-grid">
                <div class="service-card">
                    <i class="fas fa-cut service-icon"></i>
                    <h3>Expert Haircut</h3>
                    <p>Precision styling + free hair health checkup with Beardo diagnostics</p>
                    <div style="margin-top: 1rem; font-weight: 600; color: #ffd700;">Starting ₹99</div>
                </div>
                <div class="service-card">
                    <i class="fas fa-shave service-icon"></i>
                    <h3>Classic Shave</h3>
                    <p>Hot towel treatment with premium Beardo shaving cream & aftershave</p>
                    <div style="margin-top: 1rem;
